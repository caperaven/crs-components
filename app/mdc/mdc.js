import "./../../src/mdc/header/crs-header.js";

export default class MDC extends crsbinding.classes.ViewBase {
    async connectedCallback() {
        await super.connectedCallback();
    }

    // async doProcess(event) {
    //     event.target.done();
    // }
}